# Candidate tests and release gate

The one inherited command on every experiment node is:

```bash
uv sync --frozen && uv run python repro/src/run_publication_gate.py
```

The accepted scientific evidence was first generated by run
`0a874377-72e3-48af-9134-d75c4d509b10` at Git commit
`69eb2f484b32ab603f856fd0fcdee1fd960fb4ba`. The unchanged cumulative command
then passed on Hugging Face `cpu-upgrade` at commit
`4d50c350775be9896a82d3cabb56abaf261a23a4`: [Job
6a6cb983a00abefd4b289c24](https://huggingface.co/jobs/DineshAI/6a6cb983a00abefd4b289c24),
OpenResearch run `2c049206-224c-4516-93da-529c654df38a`, 2,854.836 seconds,
estimated cost `$0.0237903` at `$0.0005/min`.

Two diagnostic Jobs are retained for complete provenance: [the 3.047-second
image/setup failure](https://huggingface.co/jobs/DineshAI/6a6ca786a00abefd4b289abf)
and [the 4,390.329-second run whose science passed before the release subset
guard rejected generated text](https://huggingface.co/jobs/DineshAI/6a6ca79da00abefd4b289ac3).
Together the three CPU-only Jobs cost approximately `$0.0604018`; none used an
accelerator.

The strengthened C1--C5 evidence was accepted on [Hugging Face `cpu-upgrade`
Job 6a6d0d14a00abefd4b28a381](https://huggingface.co/jobs/DineshAI/6a6d0d14a00abefd4b28a381)
at commit `c5980d250d89368030c5ce9c160583e7f4d83460`. It ran for 5,679 seconds on
CPU only, with estimated cost `$0.047325` at `$0.0005/min`, and emitted the
verified 717,012-byte text bundle with SHA-256
`710f738a031bd1bd4d8b0a03eb4eeb067f2b50c365540122adfdc9ed5cb0c476`.

The claim-evidence gate passes C1–C6 and all legacy regressions. The repaired
release gate additionally requires:

- every required evidence file and exact verdict;
- the illustrated report and checked marimo notebook;
- the immutable judged file set as a subset of the candidate;
- a text-only upload allowlist and SHA-256 manifest;
- a byte-identical mirror of the faithful implementation inside the Space;
- an executable Space-root verifier that recomputes C1–C6;
- valid logbook JSON and reachable page paths;
- a repository and candidate secret scan.

The current live judge gives revision
`d9bd4a1dbdcefc5ae653cca2b6d0f5b7b324237a` 7/12: Claim 6 is `VERIFIED` and
Claims 1--5 are `TOY`. No score increase is claimed until the judge evaluates a
later, explicitly approved Hugging Face revision.
