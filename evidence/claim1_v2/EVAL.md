# Claim 1 — VERIFIED

The full proof-dependency certificate passed, including `1,354` exact bounded stopping-policy identities. A closed-form family over `17` thresholds attains the endpoint-corrected first inequality exactly and falsifies the leading-only mutant in every cell; the published projected bound is non-vacuous in `8` cells.
